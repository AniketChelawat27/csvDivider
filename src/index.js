import React from 'react';
import ReactDOM from 'react-dom/client';
import { CSVDividerApp } from '../components/CSVDividerApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <CSVDividerApp />
    </React.StrictMode>
); 